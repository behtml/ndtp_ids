# Краткое руководство по использованию NDTP IDS

## 🚀 Быстрый старт

### 1. Установка

```bash
git clone https://github.com/behtml/ndtp_ids.git
cd ndtp_ids
pip install -e .
```

### 2. Запуск полной системы

#### Вариант A: Два терминала

**Терминал 1 - Коллектор + Агрегатор:**
```bash
# Для Linux (замените eth0 на ваш интерфейс)
sudo python -m ndtp_ids.packet_collector | python -m ndtp_ids.aggregator --window 1

# Для Windows (требуется Npcap)
python -m ndtp_ids.packet_collector | python -m ndtp_ids.aggregator --window 1
```

**Терминал 2 - Детектор аномалий:**
```bash
python -m ndtp_ids.anomaly_detector --interval 30 --threshold 3.0
```

#### Вариант B: Интерактивное демо

```bash
python examples/demo.py
```

Выберите пункт 3 в меню для запуска полной системы.

## 📊 Просмотр результатов

### Просмотр статистики

```bash
# Через демо
python examples/demo.py
# Выбрать пункт 4

# Через SQL напрямую
sqlite3 ids.db "SELECT * FROM aggregated_metrics LIMIT 10;"
sqlite3 ids.db "SELECT * FROM alerts ORDER BY timestamp DESC LIMIT 5;"
```

### Через Python

```python
from ndtp_ids.aggregator import MetricsAggregator
from ndtp_ids.anomaly_detector import AnomalyDetector

# Получить метрики
agg = MetricsAggregator()
metrics = agg.get_metrics(limit=10)
print(metrics)

# Получить алерты
detector = AnomalyDetector()
alerts = detector.get_recent_alerts(limit=10)
for alert in alerts:
    print(f"[{alert['severity']}] {alert['description']}")
```

## 🎯 Типовые сценарии использования

### Сценарий 1: Мониторинг домашней сети

1. Запустите коллектор на вашем роутере или основном компьютере
2. Агрегатор будет собирать статистику каждые 10 минут
3. Детектор будет проверять аномалии каждую минуту
4. Алерты будут сохраняться в БД

### Сценарий 2: Обучение и демонстрация

1. Запустите систему на виртуальной машине
2. Используйте меньший размер окна (1-5 минут) для быстрых результатов
3. Генерируйте тестовый трафик для демонстрации
4. Показывайте обнаруженные аномалии в реальном времени

### Сценарий 3: Анализ pcap файлов (будущая функция)

```python
# Планируется в будущих версиях
from ndtp_ids.pcap_analyzer import analyze_pcap
results = analyze_pcap("traffic.pcap")
```

## 🔧 Настройка

### Изменение префикса локальной сети

В `src/ndtp_ids/packet_collector.py`:
```python
LOCAL_NET_PREFIX = "10.0."  # для сети 10.0.x.x
```

### Настройка временного окна

```bash
# 1 минута (для тестирования)
python -m ndtp_ids.aggregator --window 1

# 10 минут (по умолчанию)
python -m ndtp_ids.aggregator --window 10

# 1 час
python -m ndtp_ids.aggregator --window 60
```

### Настройка порога детекции

```bash
# Более чувствительный (больше алертов)
python -m ndtp_ids.anomaly_detector --threshold 2.0

# Стандартный
python -m ndtp_ids.anomaly_detector --threshold 3.0

# Менее чувствительный (меньше алертов)
python -m ndtp_ids.anomaly_detector --threshold 4.0
```

## 🐛 Устранение неполадок

### Проблема: "Permission denied" при запуске коллектора

**Решение:** Запустите с правами администратора
```bash
# Linux/macOS
sudo python -m ndtp_ids.packet_collector

# Windows
# Запустите терминал от имени администратора
```

### Проблема: Не находит сетевой интерфейс

**Решение:** Найдите имя вашего интерфейса

```bash
# Linux
ip link show
# или
ifconfig

# Windows
ipconfig
# Используйте полное имя, например "Ethernet" или "Wi-Fi"

# macOS
networksetup -listallhardwareports
```

Измените в коде:
```python
start_collector(interface="ваш_интерфейс")
```

### Проблема: Нет данных в БД

**Проверки:**
1. Коллектор запущен с правами root/admin?
2. Есть ли сетевой трафик на интерфейсе?
3. Агрегатор получает данные от коллектора?

**Отладка:**
```bash
# Проверить вывод коллектора
python -m ndtp_ids.packet_collector | head -20

# Должны увидеть JSON события
```

### Проблема: Слишком много/мало алертов

**Решение:** Настройте порог z-score

- Много ложных алертов → увеличьте порог (например, 4.0)
- Пропускаются аномалии → уменьшите порог (например, 2.5)

## 📈 Интерпретация результатов

### Типы метрик

1. **connections_count** - количество соединений
   - Нормально: 10-100 за 10 минут для обычного устройства
   - Аномалия: >1000 (возможная DDoS атака или массовая загрузка)

2. **unique_ports** - количество уникальных портов
   - Нормально: 5-20 за 10 минут
   - Аномалия: >100 (возможное сканирование портов)

3. **unique_dst_ips** - количество уникальных IP назначения
   - Нормально: 5-30 за 10 минут
   - Аномалия: >100 (возможное сканирование сети)

4. **total_bytes** - объем данных
   - Зависит от использования
   - Аномалия: резкое увеличение (возможная эксфильтрация данных)

### Уровни серьезности

- **LOW** (z-score < 3.0) - незначительное отклонение, возможно нормальное поведение
- **MEDIUM** (z-score 3.0-4.0) - заметное отклонение, требует внимания
- **HIGH** (z-score 4.0-5.0) - значительное отклонение, вероятная аномалия
- **CRITICAL** (z-score > 5.0) - критическое отклонение, требует немедленного действия

## 🎓 Образовательное использование

### Лабораторная работа 1: Захват трафика
1. Запустите коллектор
2. Откройте браузер и посетите несколько сайтов
3. Наблюдайте за событиями в консоли
4. Изучите структуру JSON событий

### Лабораторная работа 2: Агрегация данных
1. Запустите коллектор + агрегатор
2. Подождите 10-15 минут
3. Изучите данные в БД
4. Постройте графики метрик

### Лабораторная работа 3: Обнаружение аномалий
1. Запустите полную систему
2. Создайте аномальную нагрузку (много соединений)
3. Наблюдайте за алертами
4. Изучите z-score значения

### Демонстрационные сценарии

**Сценарий "Нормальный трафик":**
- Просмотр веб-страниц
- Проверка почты
- Стриминг видео

**Сценарий "Сканирование портов":**
```bash
# НЕ ИСПОЛЬЗОВАТЬ без разрешения!
# Только для демонстрации на своей машине
nmap -p 1-1000 localhost
```

**Сценарий "Массовые запросы":**
```bash
# Множественные ping запросы
for i in {1..100}; do ping -c 1 8.8.8.8 & done
```

## 🔐 Безопасность

### Что система НЕ собирает:
- ❌ Содержимое пакетов (payload)
- ❌ Пароли
- ❌ Персональные данные
- ❌ Содержимое HTTP/HTTPS трафика

### Что система собирает:
- ✅ IP адреса (источник и назначение)
- ✅ Порты (источник и назначение)
- ✅ Протокол (TCP/UDP)
- ✅ Размер пакета
- ✅ Временные метки

### Рекомендации:
- Используйте только в своих сетях
- Получайте разрешение перед мониторингом
- Защищайте базу данных от несанкционированного доступа
- Не публикуйте собранные данные

## 📞 Поддержка

При возникновении проблем:
1. Проверьте документацию (README.md)
2. Изучите примеры (examples/)
3. Запустите тесты (python -m unittest tests.test_basic)
4. Создайте issue на GitHub

## 🔗 Дополнительные ресурсы

- [Scapy документация](https://scapy.readthedocs.io/)
- [SQLite документация](https://www.sqlite.org/docs.html)
- [Z-score метод](https://en.wikipedia.org/wiki/Standard_score)
- [IDS концепции](https://en.wikipedia.org/wiki/Intrusion_detection_system)
