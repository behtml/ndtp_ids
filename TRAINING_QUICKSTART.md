# Быстрый старт: Веб-интерфейс с Suricata и обучением

## Что реализовано

Согласно требованиям из документа "Проекты ндтп - Практико-ориентированные проекты ИБ.txt", реализован полный набор функций:

### ✅ 1. Веб-интерфейс через Flask
- Dashboard с визуализацией статистики
- Мониторинг хостов в реальном времени
- Управление алертами
- Страница обучения системы

### ✅ 2. Интеграция правил Suricata (сигнатурный анализ)
- Парсер правил Suricata
- 10+ базовых правил из коробки
- Возможность добавления своих правил
- Автоматическая проверка пакетов

### ✅ 3. Поведенческий анализ
- Z-score метод для детекции аномалий
- Профилирование хостов
- Адаптивные пороги
- Объяснимость решений

### ✅ 4. Режим обучения (как начать обучать)
- Автоматическое обучение на baseline
- EWMA для адаптации
- Скользящее окно
- Защита от обучения на аномалиях

## Быстрый запуск

### Шаг 1: Установка зависимостей

```bash
cd /home/runner/work/ndtp_ids/ndtp_ids
pip install -r requirements.txt
```

### Шаг 2: Запуск демонстрации

```bash
# Демонстрация всех возможностей
python examples/integrated_demo.py
```

Вы увидите:
- ✅ Работу парсера Suricata правил
- ✅ Процесс обучения хоста
- ✅ Гибридную детекцию (Suricata + поведенческий анализ)
- ✅ Инструкции по запуску веб-интерфейса

### Шаг 3: Запуск полной системы

#### Терминал 1: Коллектор + Агрегатор
```bash
# Linux/macOS (требуется sudo для захвата пакетов)
sudo python -m ndtp_ids.packet_collector | python -m ndtp_ids.aggregator

# Windows (запустите PowerShell как администратор)
python -m ndtp_ids.packet_collector | python -m ndtp_ids.aggregator
```

#### Терминал 2: Детектор аномалий
```bash
python -m ndtp_ids.anomaly_detector --interval 30 --threshold 3.0
```

#### Терминал 3: Веб-интерфейс
```bash
python -m ndtp_ids.web_interface --port 5000
```

### Шаг 4: Открытие веб-интерфейса

Откройте в браузере: **http://localhost:5000**

## Использование системы

### Фаза 1: Обучение (Learning Mode)

Когда система впервые видит хост, он автоматически переходит в режим обучения:

1. **Сбор baseline** - первые 100 наблюдений (по умолчанию)
2. **Вычисление статистик** - среднее и стандартное отклонение
3. **Автоматический переход** в режим детекции после накопления данных

**Проверка статуса:**
- Откройте `/training` в веб-интерфейсе
- Хосты с 🟡 значком находятся в режиме обучения
- Хосты с 🟢 значком в режиме детекции

### Фаза 2: Детекция (Detection Mode)

После обучения система:

1. **Сравнивает** текущие метрики с baseline
2. **Вычисляет** z-score отклонения
3. **Генерирует** алерты при превышении порогов
4. **Адаптирует** baseline через EWMA (экспоненциальное сглаживание)

### Фаза 3: Гибридная детекция

Система использует **два метода одновременно**:

#### Метод 1: Suricata правила (сигнатурный)
- Проверка известных паттернов атак
- SSH, RDP, Telnet подключения
- Сканирование портов
- Подозрительная активность

#### Метод 2: Поведенческий анализ
- Отклонения от нормального поведения
- Новые угрозы (zero-day)
- Внутренние аномалии
- Скрытая активность

## Как начать обучать систему

### Автоматический режим (рекомендуется)

```bash
# Просто запустите систему в нормальной сети
# Обучение происходит автоматически
python -m ndtp_ids.web_interface
```

Система автоматически:
1. Обнаружит новые хосты
2. Переведет их в режим обучения
3. Соберет baseline (100 наблюдений)
4. Перейдет в режим детекции

### Ручное управление обучением

#### Через Python API:
```python
from ndtp_ids.adaptive_trainer import AdaptiveTrainer

trainer = AdaptiveTrainer()

# Перевести хост в режим обучения
trainer.set_learning_mode('192.168.1.100', enabled=True)

# Сбросить профиль и начать заново
trainer.reset_profile('192.168.1.100')

# Проверить статус
profile = trainer.get_host_profile('192.168.1.100')
print(f"Режим обучения: {profile.is_learning}")
```

#### Через REST API:
```bash
# Включить режим обучения
curl -X POST http://localhost:5000/api/host/192.168.1.100/learning \
  -H "Content-Type: application/json" \
  -d '{"enabled": true}'

# Сбросить профиль
curl -X POST http://localhost:5000/api/host/192.168.1.100/reset
```

### Настройка параметров обучения

```python
trainer = AdaptiveTrainer(
    learning_window=200,      # Больше наблюдений для обучения
    ewma_alpha=0.05,          # Медленная адаптация (0.05-0.2)
    sliding_window_size=100   # Больший исторический контекст
)
```

**Рекомендации:**
- `learning_window`: 50-200 (зависит от размера сети)
- `ewma_alpha`: 0.1 (быстрая адаптация) до 0.05 (медленная)
- `sliding_window_size`: 50-100

## Преимущества над Suricata

Из документа "Проекты ндтп - Практико-ориентированные проекты ИБ.txt":

### 1. Объяснимость решений ✅
**Suricata:** "SID 2010935 triggered"  
**NDTP IDS:** "Хост 192.168.1.100 установил в 5 раз больше соединений (50 вместо обычных 10). Это может указывать на сканирование или заражение. Z-score: 4.2"

### 2. Адаптивность к сети ✅
**Suricata:** Универсальные правила для всех  
**NDTP IDS:** Индивидуальный профиль для каждого хоста

### 3. Работа с шифрованным трафиком ✅
**Suricata:** DPI не работает с TLS  
**NDTP IDS:** Анализ метаданных (частота, объем, паттерны)

### 4. Zero-day обнаружение ✅
**Suricata:** Только известные сигнатуры  
**NDTP IDS:** Детекция новых угроз через аномалии

### 5. Контекст хоста ✅
**Suricata:** Не учитывает роль устройства  
**NDTP IDS:** Принтер ≠ сервер ≠ ноутбук

### 6. Защита от обучения на атаках ✅
Аномальные наблюдения не включаются в baseline

## Примеры использования

### Пример 1: Добавление своих правил Suricata

```python
from ndtp_ids.suricata_rules import SuricataRuleParser

parser = SuricataRuleParser()

# Добавить правило
custom_rule = 'alert tcp any any -> any 8080 (msg:"Custom Web Server"; sid:2000001;)'
parser.parse_rule(custom_rule)

# Загрузить из файла
parser.load_rules_from_file("my_rules.rules")
```

### Пример 2: Проверка конкретного пакета

```python
packet = {
    'src_ip': '192.168.1.100',
    'dst_ip': '10.0.0.50',
    'dst_port': 22,
    'protocol': 'TCP'
}

matches = parser.match_packet(packet)
for rule, reason in matches:
    print(f"ALERT: {reason}")
```

### Пример 3: Мониторинг обучения

```python
from ndtp_ids.adaptive_trainer import AdaptiveTrainer

trainer = AdaptiveTrainer()

# Получить статистику
stats = trainer.get_learning_statistics()
print(f"Хостов в обучении: {stats['learning_hosts']}")
print(f"Хостов в детекции: {stats['detection_hosts']}")

# Получить все профили
profiles = trainer.get_all_profiles()
for p in profiles:
    status = "Обучение" if p.is_learning else "Детекция"
    print(f"{p.src_ip}: {status} ({p.samples_count} наблюдений)")
```

## Структура проекта

```
ndtp_ids/
├── src/ndtp_ids/
│   ├── packet_collector.py      # Захват пакетов
│   ├── aggregator.py             # Агрегация метрик
│   ├── anomaly_detector.py       # Детекция аномалий
│   ├── suricata_rules.py         # 🆕 Парсер Suricata
│   ├── adaptive_trainer.py       # 🆕 Адаптивное обучение
│   ├── web_interface.py          # 🆕 Flask веб-интерфейс
│   └── templates/                # 🆕 HTML шаблоны
│       ├── dashboard.html
│       ├── hosts.html
│       ├── alerts.html
│       ├── rules.html
│       └── training.html
├── examples/
│   └── integrated_demo.py        # 🆕 Полная демонстрация
├── WEB_INTERFACE_GUIDE.md        # 🆕 Подробное руководство
└── TRAINING_QUICKSTART.md        # 🆕 Этот файл
```

## Диагностика

### Проблема: Хост не переходит в режим детекции

**Решение:**
```python
# Проверить количество наблюдений
profile = trainer.get_host_profile('192.168.1.100')
print(f"Наблюдений: {profile.samples_count} / {trainer.learning_window}")

# Если нужно - уменьшить порог
trainer.learning_window = 50
```

### Проблема: Слишком много ложных срабатываний

**Решение:**
```python
# Увеличить порог z-score
detector = AnomalyDetector(z_threshold=4.0)  # вместо 3.0

# Или дать больше времени на обучение
trainer.learning_window = 200
```

### Проблема: Правила Suricata не срабатывают

**Решение:**
```python
# Проверить загруженные правила
parser = SuricataRuleParser()
print(f"Загружено правил: {len(parser.rules)}")

# Проверить формат правила
rule = parser.parse_rule(rule_text)
if rule is None:
    print("Ошибка парсинга правила")
```

## API Reference

### Web Interface REST API

#### Статистика
```
GET /api/stats
```
Возвращает общую статистику системы

#### Алерты
```
GET /api/alerts?limit=50&severity=high
```
Получить список алертов

#### Хосты
```
GET /api/hosts
GET /api/host/<ip>
POST /api/host/<ip>/learning {"enabled": true}
POST /api/host/<ip>/reset
```

#### Правила Suricata
```
GET /api/suricata/rules
POST /api/suricata/rules {"rule": "alert tcp ..."}
```

## Дальнейшее развитие

- [ ] WebSocket для real-time обновлений
- [ ] Автоматическая блокировка (IPS режим)
- [ ] Экспорт правил Suricata
- [ ] Machine Learning модели
- [ ] Интеграция с внешними SIEM

## Поддержка

Для вопросов и предложений создавайте issue в репозитории.

---

**Документ создан на основе:** "Проекты ндтп - Практико-ориентированные проекты ИБ.txt"
